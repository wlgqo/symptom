<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.symptom.mapper.SurveillanceEventMapper">
    <select id="findAll" resultType="SurveillanceEvent">
        SELECT * FROM surveillance_event ORDER BY discovery_time DESC
    </select>
    <select id="findById" resultType="SurveillanceEvent">
        SELECT * FROM surveillance_event WHERE id = #{id}
    </select>
    <select id="countByStatus" resultType="int">
        SELECT COUNT(*) FROM surveillance_event WHERE status = #{status}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO surveillance_event (event_name, event_type, syndrome_type, district, venue,
            related_cases, related_warnings, status, responsible_person, description)
        VALUES (#{eventName}, #{eventType}, #{syndromeType}, #{district}, #{venue},
            #{relatedCases}, #{relatedWarnings}, #{status}, #{responsiblePerson}, #{description})
    </insert>
    <update id="update">
        UPDATE surveillance_event SET status = #{status}, responsible_person = #{responsiblePerson},
            description = #{description}, updated_at = datetime('now')
        WHERE id = #{id}
    </update>
</mapper>

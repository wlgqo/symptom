<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.symptom.mapper.OperationLogMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO operation_log (username, operation, ip) VALUES (#{username}, #{operation}, #{ip})
    </insert>
    <select id="findAll" resultType="OperationLog">
        SELECT * FROM operation_log ORDER BY created_at DESC LIMIT 100
    </select>
    <select id="findByUsername" resultType="OperationLog">
        SELECT * FROM operation_log WHERE username = #{username} ORDER BY created_at DESC
    </select>
</mapper>

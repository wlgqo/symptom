<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.symptom.mapper.SavedQueryMapper">
    <select id="findAll" resultType="SavedQuery">
        SELECT * FROM saved_query ORDER BY created_at DESC
    </select>
    <select id="findById" resultType="SavedQuery">
        SELECT * FROM saved_query WHERE id = #{id}
    </select>
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO saved_query (query_name, syndrome_type, condition_json, created_by)
        VALUES (#{queryName}, #{syndromeType}, #{conditionJson}, #{createdBy})
    </insert>
    <delete id="delete">
        DELETE FROM saved_query WHERE id = #{id}
    </delete>
</mapper>
